name: base
channels:
  - conda-forge
dependencies:
  - bc # fsl expects to find this
  - click
  - distributed=2023.8.1
  - git # needed to get version from fslanat
  - indexed_gzip
  - nibabel=5.1.0
  - numpy
  - pandas
  - prefect=2.13.8
  - pydantic
  - python>=3.11
  - tzdata
  - pip:
      - prefect-dask==0.2.5
